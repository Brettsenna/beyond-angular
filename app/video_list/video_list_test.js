'use strict';

describe('myApp.videolist module', function() {

  beforeEach(module('myApp.videolist'));

  describe('videolist controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var VideoListCtrl = $controller('VideoListCtrl');
      expect(VideoListCtrl).toBeDefined();
    }));

  });
});